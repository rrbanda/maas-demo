apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - chat-template.yaml
  - llm-inference-service.yaml
  - model-ref.yaml
  - tenant.yaml
  - auth-policy.yaml
  - subscriptions.yaml
  # PVC and download-job not needed for gemma (uses OCI modelcar image).
  # Uncomment for models that require HuggingFace download:
  # - pvc.yaml
  # - download-job.yaml
