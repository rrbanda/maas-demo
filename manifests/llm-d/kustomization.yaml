apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - inference-pool.yaml
  - inference-model.yaml
  # HTTPRoute with InferencePool backendRef - requires Gateway API Inference Extension
  # support. Uncomment when the gateway controller supports InferencePool kind.
  # See httproute.yaml header comments for details.
  # - httproute.yaml
