# vault-dev ExternalSecrets (ai-bridge-api-keys, ai-bridge-db-credentials) and
# NetworkPolicy (allow-vault-egress) are managed by the rhoai-deploy-gitops repo
# via the instance-external-secrets-config ArgoCD app.
#
# They are intentionally removed from this file to avoid SharedResourceWarnings.
# The models-as-a-service ExternalSecrets (gemini-credentials, vllm-cluster2-credentials)
# remain in external-models/external-secrets.yaml.
