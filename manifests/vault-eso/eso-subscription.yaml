# ESO operator resources (Namespace, OperatorGroup, Subscription, ExternalSecretsConfig)
# are managed by the rhoai-deploy-gitops repo via:
#   - operator-external-secrets ArgoCD app
#   - instance-external-secrets-config ArgoCD app
#
# This file intentionally left empty to avoid shared resource conflicts.
# The Vault-specific resources (SecretStore, ExternalSecrets) remain in
# secretstore.yaml and external-secrets.yaml.
