apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  # secret.yaml is excluded — maas-db-config Secret is a manual prerequisite (see secret.yaml for instructions)
  - statefulset.yaml
  - service.yaml
